'use client';

import React from 'react';

/**
 * ✅ 더미 검색 패널 (빌드 통과용)
 * 실제 ERP에서는 필터/검색 패널 역할
 */

export default function FindPanel() {
  return (
    <div className="p-2 border-b bg-gray-50 text-gray-600 text-sm">
      🔍 검색 및 필터 패널 (DB ERP 전환 중 — 임시 비활성화)
    </div>
  );
}
